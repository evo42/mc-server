# AI Rules

- All environment variables should be stored in a single `.env` file in the root directory. Do not create `.env.d` directories.
- All shell scripts should be placed in the `./cli` directory.
